driver:
#- --headless
  - --window-size=1920,1080
  - --disable-gpu
  - --page-load-strategy=eager
  - --disable-extensions
  - --disable-notifications
  - --disable-popup-blocking
  - --log-level=3
  - --disable-blink-features=AutomationControlled

general:
  url: https://sss.must.edu.tw/
  max_retry: 3
  img_path: ./imgs